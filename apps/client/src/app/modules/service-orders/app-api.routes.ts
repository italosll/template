/* eslint-disable @nx/enforce-module-boundaries */

import { environment } from "apps/client/src/environments/environment";

export const apiRoutesServiceOrders = {
  serviceOrders: `${environment.apiUrl}/service-orders`,
};
